# Session Log — 2026-03-29T14:00:00Z

## Prompts

1. Read `./log` files and `~/Downloads/plumbing.txt`; create a detailed plan for the work described.
2. Proceed with the plan.

## Actions Taken

### Item 1 — Fix FIXME in `fs/index.md`
- Replaced the `FIXME: why 8 blocks for bibliography which is only 174 bytes?` bullet with an explanation of allocation blocks, internal fragmentation, and the relationship between `ls -s` 512-byte units and 4 KiB physical blocks.

### Item 2 — Rewrite `packenv/index.md`
- Replaced the single-line FIXME stub with a full lesson covering: why package management is needed, `uv init`, `pyproject.toml` structure, `uv add`, the lock file (`uv.lock`), `uv run`, how `.venv/` works, `uv sync` for reproducibility, and exercises.

### Item 3 — Remove virtual environment content from `dock/index.md`
- Deleted the "Virtual Environments", "Package Installation", "Exercise: Substitution", and "Limits of Virtual Environments" sections (lines 7–71).
- Replaced with a short "Beyond Python Virtual Environments" aside that links to `@/packenv/`.

### Item 4 — Add env-var security to `var/index.md`
- Added two new sections at the end: "Security and Environment Variables" (covering `env`, `/proc/<pid>/environ`, `ps -E`/`ps auxe`, three rules for secrets) and "The `.env` File Convention" (covering `python-dotenv`, `.gitignore`, `.env.example`).
- Added an exercise: "Leaky Processes".

### Item 5 — Add HTTPS/TLS section to `http/index.md`
- Added four new sections after "Slicing Data": "Why HTTP Is Not Enough", "The TLS Handshake" (with figure placeholder), "What Is a Certificate?", and "HTTPS with `requests`" (including custom CA bundle and the `verify=False` antipattern).
- Added an exercise: "Certificate Errors".
- Removed the stale `FIXME` from the chapter heading.

### Item 6 — Create `ssh/index.md`
- Created `ssh/` directory and wrote a full lesson covering: what SSH is, password vs. key-pair authentication, `ssh-keygen`, `ssh-copy-id`, host key verification, `ssh-agent`, `~/.ssh/config`, `scp`/`rsync`, SSH tunnels, and `paramiko`.

### Item 7 — Create `logging/index.md`
- Created `logging/` directory and wrote a full lesson covering: why not `print()`, log levels, Python's `logging` module, loggers/handlers/formatters, `RotatingFileHandler`, structured JSON logging with `python-json-logger`, `jq` for log filtering, multi-source log aggregation (`journald`/`journalctl`), and what not to log.

### Item 8 — Update `README.md`
- Added `[Logging](@/logging/)` after "Running Jobs" (lesson 5).
- Added `[SSH](@/ssh/)` after "Certificates" (lesson 12).
- Final lesson order: Introduction, Filesystem, Processes, Running Jobs, Logging, HTTP, Authentication, Shell Variables, Packages, Networking, Certificates, SSH, Virtualization, Conclusion.

### Path Audit
- `fs/index.md`: replaced `gvwilson` with `tut` in three `ls -l` output lines.
- `proc/index.md`: replaced `gvwilson` with `tut` in `ps -a -l` output and `pstree` output.
- `var/index.md`: replaced `/Users/gregwilson/` with `/Users/tut/` in `change_path.out` (four lines).
- `dock/index.md`: replaced `#!/Users/gvwilson/conda/...` with `#!/Users/tut/conda/...` in `faker_bin.py` shebang.
- Note: Docker Hub image names (`gvwilson/ubuntu-python3` etc.) in `dock/index.md` were left unchanged — these are registry paths, not local filesystem paths, and are used as examples throughout the Docker section.
- Note: `gvwilson.github.io` URLs in `http/index.md` are real URLs used as example endpoints and were left unchanged.
